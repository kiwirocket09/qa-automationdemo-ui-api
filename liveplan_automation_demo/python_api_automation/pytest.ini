[pytest]
# Basic settings
testpaths = python_api_automation/tests
python_files = test_*.py
python_functions = test_*

# CLI Options (Log levels and display)
addopts = -v --tb=short

# Custom markers (for organizing tests)
markers =
    api: API integration tests
    smoke: Critical path tests